name: DCO Check

on:
  pull_request:
    branches: [main]

jobs:
  dco:
    name: dco
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: christopherpickering/check-dco-action@v1
